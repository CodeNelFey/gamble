# ==============================================
# CONFIGURATION DU PROJET GAMBLE
# ==============================================

# --- CONFIGURATION SERVEUR (BACKEND) ---
PORT=3001

# ADRESSE AUTORISÉE À APPELER LE SERVEUR (CORS)
# - En local/dev : met '*' (tout le monde peut se connecter)
# - En production : met l'URL de ton site (ex: http://ton-domaine.com)
ALLOWED_ORIGIN=*

# --- CONFIGURATION CLIENT (FRONTEND) ---
# L'adresse du serveur à laquelle le client React doit se connecter
# - Local : http://localhost:3001
# - Réseau (téléphone) : http://192.168.1.XX:3001 (XX = ton IP locale)
# - Prod : http://ton-domaine.com
VITE_SERVER_URL=http://localhost:3001