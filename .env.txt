# ============================================================
#  CONFIGURATION GLOBALE GAMBLE
#  Modifie ce fichier pour changer les adresses sans toucher au code
# ============================================================

# --- 1. CONFIGURATION DU SERVEUR (BACKEND) ---
# Le port sur lequel le serveur Node.js écoute
SERVER_PORT=3001

# Qui a le droit d'appeler le serveur ? (CORS)
# - En Local : *
# - En Prod : https://mon-casino.com
CORS_ORIGIN=*

# --- 2. CONFIGURATION DU CLIENT (FRONTEND) ---
# L'adresse publique de ton serveur (API + Socket)
# Le client React va lire ça pour savoir où envoyer les requêtes.

# CAS A : Local (Ton PC)
VITE_API_URL=http://localhost:3001

# CAS B : Réseau Local (Pour jouer avec ton téléphone)
# Remplace les X par ton IP locale (ex: 192.168.1.15)
# VITE_API_URL=http://192.168.1.X:3001

# CAS C : Production (VPS / Domaine)
# VITE_API_URL=https://api.mon-domaine.com
# ou si tu utilises Nginx comme proxy sur le même domaine :
# VITE_API_URL=